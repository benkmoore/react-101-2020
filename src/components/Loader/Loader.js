import React from 'react';
import { LoaderIcon, LoaderWrapper } from './Loader.styles';


export const Loader = () => {
    return (
        <LoaderWrapper>
            <LoaderIcon />
        </LoaderWrapper>
    )
}

export default Loader